//-----raviyadav144----//
#include<bits/stdc++.h>
using namespace std;

#define forn(i,j,n) for(int i =j; i<n; i++)
#define ll long long
#define all(v)  v.begin() , v.end()
#define vi(v,n) vector<int> v(n)
#define fora(e,v) for(auto &e :v)
#define endl "\n"
#define pi acos(-1.0)
#define mod 1000000007

class Solution {
public:
    vector<int> getMaximumXor(vector<int>& nums, int maximumBit) {
        int val = 0 ;
        for(int i = 0 ; i < nums.size();++i)
        {
            val ^= nums[i];
        }
        vector<int>v;
        int ans = (1 << maximumBit) -1 ;
        v.push_back(ans ^ val);
        
        for(int i = nums.size()-1 ; i >0 ;--i)
        {
            int a = val ^= nums[i];
            v.push_back(a ^ ans);
        }
        return v;
    }
};
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    // input for the fucntion
    vector<int>v;
    int n , m;
    cin>> n >> m;
    while(n--)
    {
        int a;
        cin >> a;
        v.push_back(a);
    }
    // string first , goal;
    // cin >> first >> goal;
    // int a , b ,c ;
    // cin >> a >> b >> c;
    // cout << a << b << c;
    Solution sol;
    vector<int> ans  = sol.getMaximumXor(v , m);
    fora(e , ans)
    {
        // fora(v ,e)
        // {
        //     cout << v << " ";
        // }
        cout << e << endl;
    }
    // cout << ans << endl;
    // // cout << endl;
   
}